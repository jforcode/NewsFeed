<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#000000"
    android:layout_width="@dimen/nav_drawer_width"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/nav_header_background"
        android:src="@drawable/nav_background_2"
        android:scaleType="centerInside"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

    <TextView
        android:id="@+id/sub_title"
        android:visibility="visible"
        android:textColor="@android:color/white"
        android:inputType="textUri"
        android:layout_marginLeft="@dimen/general_margin"
        android:layout_marginRight="@dimen/general_margin"
        android:layout_marginBottom="@dimen/general_margin"
        android:layout_alignBottom="@id/nav_header_background"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/title"
        android:text="Jeevan MB"
        android:textColor="@android:color/white"
        android:layout_marginLeft="@dimen/general_margin"
        android:layout_marginRight="@dimen/general_margin"
        android:layout_above="@id/sub_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile"
        android:src="@drawable/profile"
        android:layout_above="@id/title"
        android:layout_marginLeft="@dimen/general_margin"
        android:layout_marginBottom="@dimen/general_margin"
        app:civ_border_width="1dp"
        app:civ_border_color="#424242"
        android:layout_width="@dimen/profile_img_dimen"
        android:layout_height="@dimen/profile_img_dimen" />

</RelativeLayout>